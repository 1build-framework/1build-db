onebuild:
  db:
    source-path: /merge/statements
    db-info:
      data-source: common
      schema: auth
    domains:
      applications:
        db-info:
          table: applications
          id: id
        actions:
          insert:
            action-type: default
          update:
            action-type: custom
            db-info:
              statement: "update-apps.sql"
