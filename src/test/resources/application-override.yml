onebuild:
  db:
    source-path: /app/db/templates
    db-info:
      data-source: common
      schema: auth
    domains:
      applications:
        db-info:
          data-source: auth
          table: applications
          id: id
        actions:
          - name: INSERT_ONE
            action-type: default
          - name: UPDATE_BY_ID
            action-type: custom
            db-info:
              data-source: authorizations
              schema: entitlement
              table: auth-apps
              statement: "update_apps.sql"