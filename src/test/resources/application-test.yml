onebuild:
  db:
    db-info:
      data-source: common
      schema: auth
    domains:
      applications:
        db-info:
          data-source: test
          table: applications
          id: id
        actions:
          insert:
            action-type: default
          find-by-id:
            action-type: default
          find-all:
            action-type: default
          delete-by-id:
            action-type: default
          update:
            action-type: custom
            db-info:
              data-source: authorizations
              schema: auth
              table: auth-apps
              statement: "update_apps.sql"